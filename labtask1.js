 var_collection=23617;
let temp=var_collection;
let index=parseInt(prompt("Enter index.");

if (k > temp.length)
    {
       collection.write(-1);
    }
     else
    {
        var req = temp.charAt(temp.length - k)
        collection.write(Number(req));
    }
var k = 4;

console.log("print kth digit from last");
 

 
